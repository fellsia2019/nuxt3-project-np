services:
  frontend:
    image: ${DOCKER_USER}/nuxt-prod:latest
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped  # Авторестарт при падении
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000"]  # Проверяем доступность
      interval: 30s
      timeout: 5s
      retries: 3
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
